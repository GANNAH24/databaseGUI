<!DOCTYPE html>

@model MS3GUI.Models.TakenAssessment

<h2>Update Learner Score</h2>

<form asp-action="Edit">
    <div class="form-group">
        <label for="Score">New Score</label>
        <input type="number" class="form-control" id="Score" name="Score" value="@Model.Score" />
    </div>

    <button type="submit" class="btn btn-primary">Update Score</button>
</form>

</html>


@*
@model MS3GUI.Models.TakenAssessment

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>TakenAssessment</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Score" class="control-label"></label>
                <input asp-for="Score" class="form-control" />
                <span asp-validation-for="Score" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="LearnerId" />
            <input type="hidden" asp-for="AssessmentId" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
*@